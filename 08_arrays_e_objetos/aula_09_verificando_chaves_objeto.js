var aula = "Aula 09: Verificando as chaves de um objeto";
console.log(aula);

/**
 * Podemos também verificar quais as chaves cada objeto possui;
 */

let obj = {
    chave1: 1,
    chave2: 2,
    chave3: 3,
}

console.log(obj);
console.log(Object.keys(obj));
